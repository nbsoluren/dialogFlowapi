const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "root",
        database: "DialogFlow",
        multipleStatements: true
    },
    salt: "laderlappen"
}

export default config;
